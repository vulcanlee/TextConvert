我需要使用 .NET C# 建立一個專案，將其建置在 src 目錄下，使用 .NET 10 版本，若有要產生相關文件，可以在 docs 目錄下，選擇或者建立適當目錄，在其建立這些文件檔案

這個系統可以指定一個目錄，接著把這個目錄下的 office 檔案(Word, powerpoint, Excel)、PDF、Markdown、Text、HTML 格式檔案，轉換成為一個文字內容，這些文字內容為來需要作為知識庫處理之用。若為不符合條件的檔案在這個目錄內出現的時候，跳過這些檔案，不需要處理

在此，也會指定一個目錄，作為輸出目錄，將這些文字檔案內容，儲存在這裡

這裡將會建立一個 Console 類型的專案，建立一個 CLI 應用的程式，可以批次做這些檔案做轉換，這個專案可以讀取 appsettings.json 檔案，日後可以透過這個設定檔來做相關運作的設定，這個系統也需要提供日誌輸出能力，對於在執行程式過程中，各個重要的過程，都需要有日誌輸出，並且寫到不同的日誌等級 Log Level，這也包含有例外異常發生的時候。

在完成系統建立與設計之後，需要建立相關說明這個系統的文件，如 prd, change log 等等，這裡的文件格式，可以參考 "C:\Vulcan\Github\KnowledgeExtraction.AI\docs" 目錄下的文件寫作格式，也可以參考這裡分類目錄作法

using superpower skill. If you have any questions or concerns, please ask me repeatedly to confirm whether it meets my needs.